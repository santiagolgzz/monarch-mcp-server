# Monarch Money MCP Server - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: MCP API Key (for authenticating Claude mobile app requests)
# =============================================================================
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_API_KEY=your-secure-api-key-here

# =============================================================================
# REQUIRED: Monarch Money Authentication (choose one method)
# =============================================================================

# Option 1: Token-based auth (RECOMMENDED for cloud deployment)
# Get this by running: python login_setup.py
# Then copy the token from ~/.mm/mm_session.pickle or keyring
MONARCH_TOKEN=your-monarch-token-here

# Option 2: Email/password auth (for auto-login, less secure)
# MONARCH_EMAIL=your-email@example.com
# MONARCH_PASSWORD=your-password

# =============================================================================
# OPTIONAL: Server Configuration
# =============================================================================

# Server host and port
HOST=0.0.0.0
PORT=8000

# Enable debug mode (more verbose logging)
DEBUG=false
