version: 2

updates:
  # Keep GitHub Actions up to date. Actions are pinned to commit SHAs with the
  # version in a trailing comment; Dependabot updates both together.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns:
          - "*"
